LAST LEFT OFF — 2026-06-12 (end of day)
LAST ACTION: Arc turret "turret Shooter" finished as heavy mortar: barrel-driven constant-velocity
  firing (shot follows the physical barrel, solver only steers it), LOS-gated fire (5-point check),
  high-arc ballistics (min 45°), gravity 0.2x, 3x projectile exploding in ArcExplosion (r=1.5).
  Also: DevTex triplanar world-projection shader (2 units/tile), MCP read-only policy enforced via
  .claude/settings.json (writes denied — Implement Fix is the only write path).
CURRENT STATE: Turret works in TestLevel per dev playtest iterations. Fixes 030-036 written;
  dev confirmed running 031/034/035; 036 (gravity 0.2) written at session end.
NEXT STEP: Verify Fix036 was run (turret projectileGravityScale should be 0.2, not 0.5) —
  re-run it if the lob still feels heavy. Then playtest LOS gating around corners.
PENDING MANUAL STEPS: possibly Tools > Blast Frame > Implement Fix > 036 (unconfirmed);
  Fix030 (URP Lit convert) superseded by 031 (triplanar) — never needs running.
BLOCKERS / OPEN QUESTIONS: mcp-unity get_gameobject is broken in this project (bridge fails
  sending the response — "WebSocket error: An error has occurred in sending data"); use scene-file
  grep or in-fix discovery instead. Explosion is faction-agnostic (hurts other enemies/turret) —
  dev hasn't decided if that's wanted. EnemyStats.damage unused by arc projectile (explosion
  prefab owns damage).
