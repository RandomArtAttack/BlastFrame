LAST LEFT OFF — 2026-05-30 (design pass — wrapped)

LAST ACTION:
Ran a full design interview and wrote the architecture docs. Created root
DESIGN.md (architecture spec + Milestone A–F build checklist) and HANDOFF.md;
pointed CLAUDE.md at DESIGN.md and reconciled it. Deleted a misplaced empty
nested Unity project ("Blood and Bark") from the repo root.

CURRENT STATE:
- PROJECT IS GREENFIELD. The movement/combat described in CLAUDE.md was never
  actually committed here (single initial commit, no Assets/Scripts, only
  SampleScene). Earlier code was lost. Treat DESIGN.md as the build target.
- DESIGN.md is the source of truth. Decisions locked this session:
  * Areas: Mega Man weakness-web + declarative cross-area effect graph (derived
    from a saved completedAreaIds set).
  * Variability: one seeded weighted-roll (VariantSetSO) for room/spawn/enemy;
    enemy variant = VariantProfileSO applied on spawn. Single runSeed (SeedService).
  * Weapons: cycle-all, UNLIMITED, trade-off-balanced FPS types — NO energy/ammo.
  * Charge shot: discrete 3 tiers (tap + Lv1/2/3), AoE on upper tiers.
  * Player damage: contact + projectile, ~1s i-frames + flash + knockback at
    5 HP; hazards bypass i-frames.
  * Run powerups: hybrid (Minor instant / Major 1-of-3 draft).
  * MOVEMENT (build to match, DESIGN.md §4): single jump base (double-jump =
    unlockable ability); 4-unit jump w/ gravity tuned up; snappy ground;
    ground-only locked dash (input dir, horizontal), jump-cancellable;
    dash-jump maintains full dash speed, steerable, resumes on land;
    press-toward wall slide; wall jump = up + STRONG away + ~0.45s steer-lock
    => chimney-jumping is the vertical mechanic (single-wall climb impractical).

NEXT STEP:
Start coding Milestone A (Core Framework) from DESIGN.md §14, then Milestone B
(Player movement). Build one checklist item at a time in a test scene.

PENDING MANUAL STEPS:
- None in-editor yet (no code written). First editor work begins with Milestone A.
- Open tuning to settle in prototype: dash distance ~11m at speed 15.4 — keep,
  or drop to ~11 m/s for ~8m.

BLOCKERS / OPEN QUESTIONS:
- Uncommitted doc edits (energy-system removal + charge tiers + damage model)
  on branch docs/design-architecture are NOT yet committed — ask to commit.
- Not yet grilled: enemy roster, boss/phase design, HQ economy. Resume there.

DURABLE DOCS CURRENT: DESIGN.md (architecture + checklist) and HANDOFF.md
(status) at repo root. Assets/GameDesign.md (dated design log) still NOT created
this session — pending.
