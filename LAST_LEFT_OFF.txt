LAST LEFT OFF — 2026-06-20 (end of day)
LAST ACTION: Added MovingPlatform `startIndex` (start on any waypoint, then wrap per Path Mode).
  Committed + pushed the day's work on branch docs/design-architecture.
CURRENT STATE: Lava-world systems built & playtested —
  - RockBobber: bobs on the V.4 lava river ONLY (no sine fallback), reads the real mesh UV under it,
    eased "heft" via followSpeed (serialized).
  - Moving + bobbing platform: a sibling RockBobber externally-drives MovingPlatform (single mover that
    layers the bob onto the path + re-samples lava each tick). PathMode gained ReuseLoopTeleport (warp to
    the first waypoint at the end) and a serialized startIndex (begin on any waypoint).
  - Player carry fixed: PlatformRiderModule ride-grace + MoveState.JumpFired suppression (no more sliding
    off a bobbing mover); PlayerMotor depenetration pass (no tunnelling through moving kinematic platforms).
  - LavaFlowRandomizer (per-object texture offset/speed), EmissionPulse (glow throb).
  - Glass fix + dedupe WRITTEN as Implement Fix 049 / 050. Implement fixes now run to 050.
NEXT STEP: In Unity, run Implement Fix 049 (ejection glass Render Face = Both) + 050 (dedupe
  LavaFlowRandomizers) and confirm the ejection glass now renders.
PENDING MANUAL STEPS:
  - Tools > Blast Frame > Implement Fix > 049 and 050 (not yet confirmed run).
  - PC_Renderer.asset: restore the missing LavaDepthRendererFeature script OR remove the dead feature entry
    (source of the recurring "PC_Renderer is missing RendererFeatures" console error). Left manual.
  - Blender edge-split seams under light / baked-shadow lines: raise lightmap UV Pack Margin + resolution +
    Stitch Seams, or move to weighted-normals + cage bake (welded mesh) and rebake.
  - Set MovingPlatform Start Index per platform as desired.
BLOCKERS / OPEN QUESTIONS:
  - Is LavaDepthRendererFeature.cs lost (recreate it?) or should it be removed? Its script file is missing;
    it is NOT what hid the glass (stock URP Lit renders in the normal transparent pass).
